[project]
name = "ai-studio-2"
version = "0.1.0"
description = "FastAPI backend for AI Monorepo"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
]

[tool.setuptools]
packages = ["app"]
